# Backend Flask al que Nitro reenvía todo lo que llega a /api.
# El navegador nunca llama directamente a este host, así no hace falta CORS.
NUXT_API_PROXY_TARGET=http://127.0.0.1:5000

# Prefijo que usa el cliente. Solo cámbialo si sirves la API en otra ruta.
NUXT_PUBLIC_API_BASE=/api
